server:
  port: 5000
spring:
  cloud:
    gateway:
      routes:
        - id: oss
          uri: lb://codehive-oss
          predicates:
            - Path=/oss/**
          filters:
            - StripPrefix=1
